project(
  'asp', 
  'cpp',
  default_options : [
    'buildtype=debug',
    'cpp_std=c++17',
    'b_ndebug=if-release',
    'b_lto=true',
    'default_library=static',
  ],
)

subdir('meson')
subdir('src')

